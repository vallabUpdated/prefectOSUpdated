# ---- Core pipeline (Orchestrator.py) ----
langchain-anthropic>=0.3,<1.0
langchain-ollama>=0.3,<0.4
langchain-core>=0.3,<1.0
langgraph>=0.2,<1.0
anthropic>=0.40,<1.0
python-dotenv>=1.0,<2.0
python-docx>=1.1,<2.0

# ---- Web layer ----
flask>=3.0                  # server.py (dashboard backend, :5055)
flask-cors>=4.0
psutil>=5.9                 # server.py /app-status process checks
fastapi>=0.110              # batch_api.py (batch/email/governance API, :8000)
uvicorn[standard]>=0.29
sse-starlette>=2.0

# ---- Document processing / RAG ----
pdfplumber>=0.11
pypdf>=4.0                  # PDF fallback in email_validation.py / loan_processing.py
pandas>=2.0                 # batch_ingest/extractor.py
chromadb>=1.0               # rag_pipeline.py

# ---- Email intake (email_ingest.py / email_validation.py) ----
google-auth>=2.0            # oauth_google mailboxes (service-account XOAUTH2)
dnspython>=2.0              # SPF lookups
dkimpy>=1.1                 # DKIM verification

# ---- Optional queue backends (batch_ingest) ----
redis>=5.0
boto3>=1.34                 # SQS / S3

# ---- Tests ----
pytest>=8.0
httpx>=0.27                 # fastapi TestClient
playwright>=1.40            # e2e_test_*.py, tests/e2e_parallel_runs.py  (then: python -m playwright install chromium)
reportlab>=4.0              # tests/make_sample_packs.py
